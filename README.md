# Scalable Job Import System

This repository contains a full-stack job import system built using the **MERN stack**, with background queue processing and import history tracking.

## Project Structure

